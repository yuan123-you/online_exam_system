server:
  port: ${PORT:8080}
  error:
    whitelabel:
      enabled: false

spring:
  datasource:
    url: ${MYSQL_URL:jdbc:mysql://localhost:3306/online_exam_system?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false&tinyInt1isBit=false}
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:123456}
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never
      encoding: UTF-8
      continue-on-error: true

ai:
  api-url: https://open.bigmodel.cn/api/paas/v4/chat/completions
  api-key: 82f20bac24a545c595dc30f7eee3dfd1.eJmxuTgiNAxaDNEX
  model: glm-4.7-flash
  timeout-ms: 120000
  rate-limit-per-minute: 10
  concurrent-limit: 4
  thread-pool:
    core-size: 4
    max-size: 16
    queue-capacity: 100
    keep-alive-seconds: 60

logging:
  level:
    org.springframework.jdbc.datasource.init: warn
